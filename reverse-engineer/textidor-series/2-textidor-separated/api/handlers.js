const fs = require('fs');
const path = require('path');
const config = require('../config');

// define FILES_DIR
const FILES_DIR = path.join(_);

// declare the handlers
const handlers = {};

// export the handlers
_;
