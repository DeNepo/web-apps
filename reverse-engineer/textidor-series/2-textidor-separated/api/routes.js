// require the handlers
_;
const express = require('express');

// build the router
_;

router.get('/', (req, res) => {
  res.send('files API!');
});

// add routes to router

// export the router
_;
